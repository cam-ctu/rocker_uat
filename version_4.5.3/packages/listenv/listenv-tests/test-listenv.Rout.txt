
R version 4.5.3 (2026-03-11) -- "Reassured Reassurer"
Copyright (C) 2026 The R Foundation for Statistical Computing
Platform: x86_64-pc-linux-gnu

R is free software and comes with ABSOLUTELY NO WARRANTY.
You are welcome to redistribute it under certain conditions.
Type 'license()' or 'licence()' for distribution details.

R is a collaborative project with many contributors.
Type 'contributors()' for more information and
'citation()' on how to cite R or R packages in publications.

Type 'demo()' for some demos, 'help()' for on-line help, or
'help.start()' for an HTML browser interface to help.
Type 'q()' to quit R.

> #! /usr/bin/env Rscript
> ## This runs testme test script inst/testme/test-listenv.R
> ## Don't edit - it was autogenerated by inst/testme/deploy.R
> listenv:::testme("listenv")
Test ‘listenv’ ...
 chr "none"

> library("listenv")

> ovars <- ls(envir = globalenv())

> oopts <- options(warn = 1)

> with_r_330 <- function(expr) {
+     if (getRversion() < "3.3.0") 
+         return()
+     eval(substitute(expr), envir = parent.frame(), enclos =  .... [TRUNCATED] 

> x <- listenv()

> print(x)
A ‘listenv’ vector with 0 elements (unnamed).

> stopifnot(length(x) == 0)

> stopifnot(is.null(names(x)))

> with_r_330({
+     stopifnot(identical(lengths(x), integer(0)))
+ })

> x <- listenv()

> names(x) <- character(0)

> print(x)
A ‘listenv’ vector with 0 elements (named).

> x <- listenv(a = 1)

> print(x)
A ‘listenv’ vector with 1 element (‘a’).

> stopifnot(length(x) == 1)

> stopifnot(identical(names(x), c("a")))

> stopifnot(identical(x$a, 1))

> with_r_330({
+     stopifnot(identical(lengths(x), c(a = 1)))
+ })

> x <- listenv(a = 1, b = 2:3)

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(x$a, 1), identical(x$b, 2:3))

> with_r_330({
+     stopifnot(identical(lengths(x), c(a = 1, b = 2)))
+     stopifnot(identical(lengths(x, use.names = FALSE), c(1, 2)))
+ })

> x <- listenv(b = 2:3, .a = 1)

> print(x)
A ‘listenv’ vector with 2 elements (‘b’, ‘.a’).

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("b", ".a")))

> stopifnot(identical(x$.a, 1), identical(x$b, 2:3))

> with_r_330({
+     stopifnot(identical(lengths(x), c(b = 2, .a = 1)))
+ })

> x <- listenv(length = 3, a = 1)

> print(x)
A ‘listenv’ vector with 2 elements (‘length’, ‘a’).

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("length", "a")))

> stopifnot(identical(x$length, 3), identical(x$a, 1))

> with_r_330({
+     stopifnot(identical(lengths(x), c(length = 1, a = 1)))
+ })

> x <- listenv()

> print(x)
A ‘listenv’ vector with 0 elements (unnamed).

> print(length(x))
[1] 0

> print(names(x))
NULL

> stopifnot(length(x) == 0)

> x$a <- 1

> print(x)
A ‘listenv’ vector with 1 element (‘a’).

> print(length(x))
[1] 1

> print(names(x))
[1] "a"

> stopifnot(length(x) == 1)

> stopifnot(identical(names(x), c("a")))

> stopifnot(identical(x$a, 1), is.null(x$b))

> x$b <- 2

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(x$b, 2))

> x$a <- 0

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(x[["a"]], 0))

> x$a <- 1

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(x$a, 1))

> with_r_330({
+     stopifnot(identical(lengths(x), c(a = 1, b = 1)))
+ })

> x[["a"]] <- 0

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> key <- "b"

> x[[key]] <- 3

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(x$b, 3), identical(x[["b"]], 3), 
+     identical(x[[key]], 3))

> x[[3]] <- 3.14

> print(x)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘’).

> print(length(x))
[1] 3

> print(names(x))
[1] "a" "b" "" 

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "")))

> stopifnot(identical(x[[3]], 3.14))

> names(x) <- c("a", "b", "c")

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "c")))

> stopifnot(identical(x[[3]], 3.14), identical(x[["c"]], 
+     3.14), identical(x$c, 3.14))

> with_r_330({
+     stopifnot(identical(lengths(x), c(a = 1, b = 1, c = 1)))
+ })

> x <- listenv()

> x[1:3] <- list(1, NULL, 3)

> print(x)
A ‘listenv’ vector with 3 elements (unnamed).

> stopifnot(is.null(names(x)))

> with_r_330({
+     stopifnot(identical(lengths(x), c(1, 0, 1)))
+ })

> y <- x[]

> print(y)
A ‘listenv’ vector with 3 elements (unnamed).

> stopifnot(length(y) == length(x))

> stopifnot(all.equal(y, x))

> stopifnot(!identical(y, x))

> stopifnot(is.null(names(y)))

> with_r_330({
+     stopifnot(identical(lengths(y), c(1, 0, 1)))
+ })

> y <- x[1]

> print(y)
A ‘listenv’ vector with 1 element (unnamed).

> stopifnot(is.null(names(y)))

> y <- x[2:3]

> print(y)
A ‘listenv’ vector with 2 elements (unnamed).

> stopifnot(is.null(names(y)))

> with_r_330({
+     stopifnot(identical(lengths(y), c(0, 1)))
+ })

> y <- x[-1]

> print(y)
A ‘listenv’ vector with 2 elements (unnamed).

> stopifnot(is.null(names(y)))

> with_r_330({
+     stopifnot(identical(lengths(y), c(0, 1)))
+ })

> x[c("c", ".a", "b")] <- list(NULL, 3, 1)

> print(x)
A ‘listenv’ vector with 6 elements (‘’, ‘’, ‘’, ..., ‘b’).

> stopifnot(identical(names(x), c("", "", "", "c", ".a", 
+     "b")))

> with_r_330({
+     stopifnot(identical(lengths(x), c(1, 0, 1, c = 0, .a = 1, 
+         b = 1)))
+ })

> y <- as.list(x)

> str(y)
List of 6
 $   : num 1
 $   : NULL
 $   : num 3
 $ c : NULL
 $ .a: num 3
 $ b : num 1

> stopifnot(identical(names(y), c("", "", "", "c", ".a", 
+     "b")))

> y <- as.list(x, all.names = FALSE)

> str(y)
List of 5
 $  : num 1
 $  : NULL
 $  : num 3
 $ c: NULL
 $ b: num 1

> stopifnot(identical(names(y), c("", "", "", "c", "b")))

> y <- as.list(x, sorted = TRUE)

> str(y)
List of 6
 $   : num 1
 $   : NULL
 $   : num 3
 $ .a: num 3
 $ b : num 1
 $ c : NULL

> stopifnot(identical(names(y), c("", "", "", ".a", 
+     "b", "c")))

> y <- as.list(x, all.names = FALSE, sorted = TRUE)

> str(y)
List of 5
 $  : num 1
 $  : NULL
 $  : num 3
 $ b: num 1
 $ c: NULL

> stopifnot(identical(names(y), c("", "", "", "b", "c")))

> x <- as.listenv(1:3)

> y <- as.list(x, all.names = FALSE)

> stopifnot(length(y) == 3, identical(y, as.list(x)))

> x <- listenv()

> x[c("a", "b", "c")] <- list(1, NULL, 3)

> stopifnot(length(x) == 3)

> y <- x[NULL]

> print(y)
A ‘listenv’ vector with 0 elements (unnamed).

> z <- as.list(y)

> print(z)
list()

> stopifnot(identical(z, list()))

> y <- x[integer(0)]

> print(y)
A ‘listenv’ vector with 0 elements (unnamed).

> z <- as.list(y)

> print(z)
list()

> stopifnot(identical(z, list()))

> y <- x["a"]

> print(y)
A ‘listenv’ vector with 1 element (‘a’).

> z <- as.list(y)

> print(z)
$a
[1] 1


> stopifnot(identical(z, list(a = 1)))

> y <- x[c("a", "c")]

> print(y)
A ‘listenv’ vector with 2 elements (‘a’, ‘c’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$c
[1] 3


> stopifnot(identical(z, list(a = 1, c = 3)))

> y <- x[c("c", "a")]

> print(y)
A ‘listenv’ vector with 2 elements (‘c’, ‘a’).

> z <- as.list(y)

> print(z)
$c
[1] 3

$a
[1] 1


> stopifnot(identical(z, list(c = 3, a = 1)))

> y <- x[c(1, 3)]

> print(y)
A ‘listenv’ vector with 2 elements (‘a’, ‘c’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$c
[1] 3


> stopifnot(identical(z, list(a = 1, c = 3)))

> y <- x[-2]

> print(y)
A ‘listenv’ vector with 2 elements (‘a’, ‘c’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$c
[1] 3


> stopifnot(identical(z, list(a = 1, c = 3)))

> y <- x[-c(1, 3)]

> print(y)
A ‘listenv’ vector with 1 element (‘b’).

> z <- as.list(y)

> print(z)
$b
NULL


> stopifnot(identical(z, list(b = NULL)))

> y <- x[rep(1, times = 6)]

> print(y)
A ‘listenv’ vector with 6 elements (‘a’, ‘a’, ‘a’, ..., ‘a’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$a
[1] 1

$a
[1] 1

$a
[1] 1

$a
[1] 1

$a
[1] 1


> stopifnot(identical(z, rep(list(a = 1), times = 6)))

> y <- x[c(4, 3)]

> print(y)
A ‘listenv’ vector with 2 elements (‘’, ‘c’).

> stopifnot(identical(names(y), c("", "c")))

> z <- as.list(y)

> print(z)
[[1]]
NULL

$c
[1] 3


> stopifnot(identical(names(z), c("", "c")))

> stopifnot(identical(z, list(NULL, c = 3)))

> y <- x[1:10]

> print(y)
A ‘listenv’ vector with 10 elements (‘a’, ‘b’, ‘c’, ..., ‘’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$b
NULL

$c
[1] 3

[[4]]
NULL

[[5]]
NULL

[[6]]
NULL

[[7]]
NULL

[[8]]
NULL

[[9]]
NULL

[[10]]
NULL


> stopifnot(identical(z, c(as.list(x), rep(list(NULL), 
+     times = 7))))

> y <- x[c(TRUE, FALSE, TRUE)]

> print(y)
A ‘listenv’ vector with 2 elements (‘a’, ‘c’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$c
[1] 3


> stopifnot(identical(z, list(a = 1, c = 3)))

> y <- x[c(TRUE, FALSE)]

> print(y)
A ‘listenv’ vector with 2 elements (‘a’, ‘c’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$c
[1] 3


> stopifnot(identical(z, list(a = 1, c = 3)))

> y <- x[TRUE]

> print(y)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘c’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$b
NULL

$c
[1] 3


> stopifnot(identical(z, as.list(x)))

> y <- x[FALSE]

> print(y)
A ‘listenv’ vector with 0 elements (unnamed).

> z <- as.list(y)

> print(z)
list()

> stopifnot(identical(z, list()))

> y <- x[rep(TRUE, times = 5)]

> print(y)
A ‘listenv’ vector with 5 elements (‘a’, ‘b’, ‘c’, ‘’, ‘’).

> z <- as.list(y)

> print(z)
$a
[1] 1

$b
NULL

$c
[1] 3

[[4]]
NULL

[[5]]
NULL


> stopifnot(identical(z, c(as.list(x), list(NULL), list(NULL))))

> with_r_330({
+     stopifnot(identical(lengths(z), c(a = 1, b = 0, c = 1, 0, 
+         0)))
+ })

> x <- listenv(a = 1, b = 2, c = 3.14)

> y <- local({
+     x[[3]]
+ })

> stopifnot(identical(y, 3.14))

> y <- local({
+     x[3]
+ })

> stopifnot(identical(y[[1]], 3.14))

> y <- local({
+     ii <- 3
+     x[[ii]]
+ })

> stopifnot(identical(y, 3.14))

> y <- local({
+     ii <- 3
+     x[ii]
+ })

> stopifnot(identical(y[[1]], 3.14))

> local({
+     x[[3]] <- 42
+ })

> y <- x[[3]]

> stopifnot(identical(y, 42))

> local({
+     x[3] <- 3.14
+ })

> y <- x[[3]]

> stopifnot(identical(y, 3.14))

> local({
+     ii <- 3
+     x[ii] <- 42
+ })

> y <- x[[3]]

> stopifnot(identical(y, 42))

> local({
+     ii <- 3
+     x[[ii]] <- 3.14
+ })

> y <- x[[3]]

> stopifnot(identical(y, 3.14))

> x[["a"]] <- NULL

> print(x)
A ‘listenv’ vector with 2 elements (‘b’, ‘c’).

> print(length(x))
[1] 2

> print(names(x))
[1] "b" "c"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("b", "c")))

> x[[3]] <- NULL

> print(x)
A ‘listenv’ vector with 2 elements (‘b’, ‘c’).

> print(length(x))
[1] 2

> print(names(x))
[1] "b" "c"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("b", "c")))

> x[[2]] <- NULL

> print(x)
A ‘listenv’ vector with 1 element (‘b’).

> print(length(x))
[1] 1

> print(names(x))
[1] "b"

> stopifnot(length(x) == 1)

> stopifnot(identical(names(x), c("b")))

> x$b <- NULL

> print(x)
A ‘listenv’ vector with 0 elements (named).

> print(length(x))
[1] 0

> print(names(x))
character(0)

> stopifnot(length(x) == 0)

> x[2] <- list(NULL)

> print(x)
A ‘listenv’ vector with 2 elements (‘’, ‘’).

> print(length(x))
[1] 2

> print(names(x))
[1] "" ""

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("", "")))

> x["c"] <- list(NULL)

> print(x)
A ‘listenv’ vector with 3 elements (‘’, ‘’, ‘c’).

> print(length(x))
[1] 3

> print(names(x))
[1] ""  ""  "c"

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("", "", "c")))

> x <- listenv()

> x[c("a", "b", "c")] <- 1:3

> print(x)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘c’).

> str(as.list(x))
List of 3
 $ a: int 1
 $ b: int 2
 $ c: int 3

> print(length(x))
[1] 3

> print(names(x))
[1] "a" "b" "c"

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "c")))

> stopifnot(identical(as.list(x), list(a = 1, b = 2, 
+     c = 3)))

> stopifnot(identical(unlist(x), c(a = 1, b = 2, c = 3)))

> x[] <- 3:1

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "c")))

> stopifnot(identical(as.list(x), list(a = 3, b = 2, 
+     c = 1)))

> x[c("c", "b")] <- 2:3

> print(x)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘c’).

> str(as.list(x))
List of 3
 $ a: int 3
 $ b: int 3
 $ c: int 2

> print(length(x))
[1] 3

> print(names(x))
[1] "a" "b" "c"

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "c")))

> stopifnot(identical(as.list(x), list(a = 3, b = 3, 
+     c = 2)))

> x[c("a", "c")] <- 1

> print(x)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘c’).

> str(as.list(x))
List of 3
 $ a: int 1
 $ b: int 3
 $ c: int 1

> print(length(x))
[1] 3

> print(names(x))
[1] "a" "b" "c"

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "c")))

> stopifnot(identical(as.list(x), list(a = 1, b = 3, 
+     c = 1)))

> x[c("d", "e")] <- 4:5

> print(x)
A ‘listenv’ vector with 5 elements (‘a’, ‘b’, ‘c’, ‘d’, ‘e’).

> str(as.list(x))
List of 5
 $ a: int 1
 $ b: int 3
 $ c: int 1
 $ d: int 4
 $ e: int 5

> print(length(x))
[1] 5

> print(names(x))
[1] "a" "b" "c" "d" "e"

> stopifnot(length(x) == 5)

> stopifnot(identical(names(x), c("a", "b", "c", "d", 
+     "e")))

> stopifnot(identical(as.list(x), list(a = 1, b = 3, 
+     c = 1, d = 4, e = 5)))

> x <- listenv()

> x[c("a", "b")] <- 1:2

> x[c(TRUE, FALSE)] <- 2

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> str(as.list(x))
List of 2
 $ a: int 2
 $ b: int 2

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(as.list(x), list(a = 2, b = 2)))

> x[c(TRUE)] <- 1

> print(x)
A ‘listenv’ vector with 2 elements (‘a’, ‘b’).

> str(as.list(x))
List of 2
 $ a: int 1
 $ b: int 1

> print(length(x))
[1] 2

> print(names(x))
[1] "a" "b"

> stopifnot(length(x) == 2)

> stopifnot(identical(names(x), c("a", "b")))

> stopifnot(identical(as.list(x), list(a = 1, b = 1)))

> x[c(TRUE, FALSE, TRUE, FALSE)] <- 3

> print(x)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘’).

> str(as.list(x))
List of 3
 $ a: int 3
 $ b: int 1
 $  : int 3

> print(length(x))
[1] 3

> print(names(x))
[1] "a" "b" "" 

> stopifnot(length(x) == 3)

> stopifnot(identical(names(x), c("a", "b", "")))

> stopifnot(identical(as.list(x), list(a = 3, b = 1, 
+     3)))

> x <- as.listenv(1:6)

> names(x) <- letters[seq_along(x)]

> y <- as.list(x)

> stopifnot(identical(as.list(x), y))

> x[2] <- NULL

> y[2] <- NULL

> stopifnot(identical(as.list(x), y))

> x[4:3] <- NULL

> y[4:3] <- NULL

> stopifnot(identical(as.list(x), y))

> x[rep(2, times = 10)] <- NULL

> y[rep(2, times = 10)] <- NULL

> stopifnot(identical(as.list(x), y))

> y[] <- NULL

> x[] <- NULL

> stopifnot(identical(as.list(x), y))

> x <- as.listenv(1:6)

> names(x) <- letters[seq_along(x)]

> y <- as.list(x)

> stopifnot(identical(as.list(x), y))

> x[c(TRUE, FALSE)] <- NULL

> y[c(TRUE, FALSE)] <- NULL

> stopifnot(identical(as.list(x), y))

> x[c("b", "f")] <- NULL

> y[c("b", "f")] <- NULL

> stopifnot(identical(as.list(x), y))

> x <- listenv()

> for (ii in 1:3) {
+     x[[ii]] <- letters[ii]
+     print(x[[ii]])
+ }
[1] "a"
[1] "b"
[1] "c"

> print(x)
A ‘listenv’ vector with 3 elements (unnamed).

> names(x) <- sprintf("item%d", seq_along(x))

> print(x)
A ‘listenv’ vector with 3 elements (‘item1’, ‘item2’, ‘item3’).

> y <- as.list(x)

> str(y)
List of 3
 $ item1: chr "a"
 $ item2: chr "b"
 $ item3: chr "c"

> stopifnot(identical(names(y), c("item1", "item2", 
+     "item3")))

> stopifnot(identical(y[[1]], "a"), identical(y[[2]], 
+     "b"), identical(y[[3]], "c"))

> x[[2]] <- "B"

> stopifnot(identical(x$item2, "B"))

> x <- listenv()

> x[[1]] <- {
+     1
+ }

> x[[3]] <- {
+     "Hello world!"
+ }

> stopifnot(length(x) == 3)

> stopifnot(identical(seq_along(x), seq_len(length(x))))

> print(x)
A ‘listenv’ vector with 3 elements (unnamed).

> names(x) <- c("a", "b", "c")

> print(x)
A ‘listenv’ vector with 3 elements (‘a’, ‘b’, ‘c’).

> x$b <- TRUE

> stopifnot(identical(x[[1]], 1))

> stopifnot(identical(x[[2]], TRUE))

> stopifnot(identical(x$b, TRUE))

> stopifnot(identical(x[["b"]], TRUE))

> y <- as.list(x)

> str(y)
List of 3
 $ a: num 1
 $ b: logi TRUE
 $ c: chr "Hello world!"

> stopifnot(length(y) == 3)

> x <- listenv()

> x$a <- 1

> x[[3]] <- 3

> print(names(x))
[1] "a" ""  "" 

> stopifnot(identical(names(x), c("a", "", "")))

> var <- get_variable(x, "a")

> stopifnot(var == "a")

> var <- get_variable(x, 1)

> stopifnot(var == "a")

> var <- get_variable(x, 3)

> stopifnot(var != "c")

> names(x) <- c("a", "b", "c")

> var <- get_variable(x, 3)

> stopifnot(var != "c")

> var <- get_variable(x, "c")

> stopifnot(var != "c")

> x$b <- 2

> var <- get_variable(x, 2)

> stopifnot(var == "b")

> var <- get_variable(x, "b")

> stopifnot(var == "b")

> x <- listenv()

> x[["1"]] <- 1

> x[["3"]] <- 3

> print(names(x))
[1] "1" "3"

> stopifnot(identical(names(x), c("1", "3")))

> x <- listenv(a = 1, b = 2)

> length(x) <- 2

> stopifnot(length(x) == 2)

> x <- listenv()

> stopifnot(length(x) == 0)

> length(x) <- 3

> stopifnot(length(x) == 3)

> stopifnot(is.null(names(x)))

> names(x) <- c("a", "b", "c")

> x$a <- 2

> stopifnot(identical(x$a, 2))

> x[c("a", "c")] <- c(2, 1)

> stopifnot(identical(x$a, 2), identical(x$c, 1))

> length(x) <- 4

> stopifnot(length(x) == 4)

> stopifnot(identical(names(x), c("a", "b", "c", "")))

> length(x) <- 1

> stopifnot(length(x) == 1)

> stopifnot(identical(names(x), c("a")))

> stopifnot(identical(x$a, 2))

> length(x) <- 0

> stopifnot(length(x) == 0)

> stopifnot(length(names(x)) == 0)

> for (recursive in c(FALSE, TRUE)) {
+     x <- list()
+     x$a <- list(B = 1:3)
+     x$b <- list(C = 1:3, D = 4:5)
+     y1 <- unlist(x, recursive .... [TRUNCATED] 

> x <- listenv()

> x$a <- list(B = 1:3)

> x$b <- as.listenv(list(C = 1:3, D = 4:5))

> y3 <- unlist(x, recursive = TRUE)

> stopifnot(identical(y3, y1))

> x <- listenv()

> y <- unlist(x)

> stopifnot(length(y) == 0)

> stopifnot(is.null(y))

> x <- listenv(c = NULL, .a = 3, b = 1)

> print(x)
A ‘listenv’ vector with 3 elements (‘c’, ‘.a’, ‘b’).

> stopifnot(all.equal(x, x))

> stopifnot(all.equal(x, list(c = NULL, .a = 3, b = 1)))

> stopifnot(all.equal(x, list(c = NULL, .a = 3, b = 1), 
+     sorted = TRUE))

> stopifnot(all.equal(x, list(.a = 3, b = 1, c = NULL), 
+     sorted = TRUE))

> stopifnot(all.equal(x, list(c = NULL, b = 1), all.names = FALSE))

> stopifnot(all.equal(x, list(.a = 3, c = NULL, b = 1), 
+     all.names = FALSE))

> stopifnot(all.equal(x, list(b = 1, c = NULL), all.names = FALSE, 
+     sorted = TRUE))

> res <- all.equal(x, list(b = 1, c = NULL), sorted = FALSE)

> stopifnot(!isTRUE(res))

> res <- all.equal(x, list(b = 1, c = NULL), all.names = FALSE)

> stopifnot(!isTRUE(res))

> y <- as.list(x)

> stopifnot(identical(names(y), names(x)))

> z <- as.listenv(y)

> stopifnot(identical(names(z), names(y)))

> stopifnot(all.equal(x, y))

> x <- listenv()

> x[1:3] <- 1:3

> res <- tryCatch(x[1:2] <- 1:4, warning = function(w) {
+     class(w) <- "try-warning"
+     w
+ })

> stopifnot(inherits(res, "try-warning"))

> res <- tryCatch(x[1:3] <- 1:2, warning = function(w) {
+     class(w) <- "try-warning"
+     w
+ })

> stopifnot(inherits(res, "try-warning"))

> res <- tryCatch(x[integer(0)] <- 1, warning = function(w) {
+     class(w) <- "try-warning"
+     w
+ })

> stopifnot(!inherits(res, "try-warning"))

> x <- listenv()

> length(x) <- 3

> names(x) <- c("a", "b", "c")

> res <- try(names(x) <- c("a", "b"), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[1:2]], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[0]], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[length(x) + 1]], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[1 + (0+2i)]], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[1 + (0+2i)], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[1 + (0+2i)]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[1 + (0+2i)] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[integer(0)]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[1:2]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[Inf]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[0]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[-1]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[character(0)]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[c("a", "b")]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[""]] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv(a = 1)

> print(x)
A ‘listenv’ vector with 1 element (‘a’).

> x <- listenv()

> names(x) <- character(0)

> print(x)
A ‘listenv’ vector with 0 elements (named).

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> dimnames(x) <- list(c("r1", "r2"), c("c1", "c2", "c3"))

> print(x)
A ‘listenv’ matrix with 6 elements (unnamed) arranged in 2x3 rows (‘r1’, ‘r2’) and columns (‘c1’, ‘c2’, ‘c3’).

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> dimnames(x) <- list(c("r1", "r2"), NULL)

> print(x)
A ‘listenv’ matrix with 6 elements (unnamed) arranged in 2x3 rows (‘r1’, ‘r2’) and unnamed columns.

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> dimnames(x) <- list(NULL, c("c1", "c2", "c3"))

> print(x)
A ‘listenv’ matrix with 6 elements (unnamed) arranged in 2x3 unnamed rows and columns (‘c1’, ‘c2’, ‘c3’).

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> dimnames(x) <- list(NULL, NULL)

> print(x)
A ‘listenv’ matrix with 6 elements (unnamed) arranged in 2x3 unnamed rows and columns.

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> print(x)
A ‘listenv’ matrix with 6 elements (unnamed) arranged in 2x3 unnamed rows and columns.

> x <- as.listenv(1:24)

> dim(x) <- c(2, 3, 4)

> dimnames(x) <- list(letters[1:2], letters[1:3], letters[1:4])

> print(x)
A ‘listenv’ array with 24 elements (unnamed) arranged in 2x3x4 dimensions (#1: ‘a’, ‘b’; #2: ‘a’, ‘b’, ‘c’; #3: ‘a’, ‘b’, ‘c’, ‘d’).

> x <- as.listenv(1:24)

> dim(x) <- c(2, 3, 4)

> dimnames(x) <- list(letters[1:2], NULL, letters[1:4])

> print(x)
A ‘listenv’ array with 24 elements (unnamed) arranged in 2x3x4 partially named dimensions (#1: ‘a’, ‘b’; #2: NULL; #3: ‘a’, ‘b’, ‘c’, ‘d’).

> x <- as.listenv(1:24)

> dim(x) <- c(2, 3, 4)

> print(x)
A ‘listenv’ array with 24 elements (unnamed) arranged in 2x3x4 unnamed dimensions.

> x <- as.listenv(1:24)

> dim(x) <- c(2, 3, 4)

> dimnames(x) <- list(NULL, NULL, NULL)

> print(x)
A ‘listenv’ array with 24 elements (unnamed) arranged in 2x3x4 unnamed dimensions.

> x <- listenv(a = 1, b = 2)

> res <- try(map(x), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv()

> length(x) <- 3

> names(x) <- c("a", "b", "c")

> x$nonexistent <- NULL

> x[[10]] <- NULL

> res <- try({
+     x[1:2] <- list()
+ }, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv(a = 1, b = 2)

> stopifnot(isTRUE(all.equal(x, x)))

> x <- listenv(a = 1, b = 2)

> stopifnot(is.null(x[["nonexistent"]]))

> x <- listenv()

> length(x) <- 3

> stopifnot(is.null(x[[2]]))

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> res <- try(x[1, 2, 3], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[1, 2, 3] <- 1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[, ] <- NULL, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[c(-1, 1)], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> e <- new.env(parent = emptyenv())

> e$x <- 1

> e$y <- 2

> x <- as.listenv(e)

> stopifnot(length(x) == 2)

> y <- as.list(x)

> stopifnot(all(sort(names(y)) == c("x", "y")))

> x <- listenv(a = 1, b = 2)

> stopifnot(is.null(x[["nonexistent"]]))

> x <- listenv()

> res <- try(length(x) <- -1, silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv(a = 1)

> res <- try(listenv::map(x), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> Sys.setenv(R_LISTENV_MAP_DEPRECATED = "deprecated")

> res <- withCallingHandlers(listenv::map(x), warning = function(w) {
+     invokeRestart("muffleWarning")
+ })

> stopifnot(is.character(res))

> Sys.unsetenv("R_LISTENV_MAP_DEPRECATED")

> x <- listenv()

> x[1:3] <- 1:3

> res <- try(x[c(1, -1)], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv()

> x[1:3] <- 1:3

> res <- try(x[1] <- list(), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> options(oopts)

> rm(list = setdiff(ls(envir = globalenv()), ovars), 
+     envir = globalenv())
Skipping, because path appears not to be an 'R CMD check' folder: ‘/tmp’
Test time: user.self=0.2s, sys.self=0.004s, elapsed=0.2s, user.child=0s, sys.child=0s
Test ‘listenv’ ... success
> 
