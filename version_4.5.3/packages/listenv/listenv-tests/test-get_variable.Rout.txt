
R version 4.5.3 (2026-03-11) -- "Reassured Reassurer"
Copyright (C) 2026 The R Foundation for Statistical Computing
Platform: x86_64-pc-linux-gnu

R is free software and comes with ABSOLUTELY NO WARRANTY.
You are welcome to redistribute it under certain conditions.
Type 'license()' or 'licence()' for distribution details.

R is a collaborative project with many contributors.
Type 'contributors()' for more information and
'citation()' on how to cite R or R packages in publications.

Type 'demo()' for some demos, 'help()' for on-line help, or
'help.start()' for an HTML browser interface to help.
Type 'q()' to quit R.

> #! /usr/bin/env Rscript
> ## This runs testme test script inst/testme/test-get_variable.R
> ## Don't edit - it was autogenerated by inst/testme/deploy.R
> listenv:::testme("get_variable")
Test ‘get_variable’ ...
 chr "none"

> library("listenv")

> ovars <- ls(envir = globalenv())

> oopts <- options(warn = 1)

> x <- listenv()

> length(x) <- 3

> names(x) <- c("a", "b", "c")

> stopifnot(length(x) == 3)

> print(mapping(x))
 a  b  c 
NA NA NA 

> var <- get_variable(x, "a")

> stopifnot(!is.na(var))

> stopifnot(length(x) == 3)

> print(mapping(x))
 a  b  c 
NA NA NA 

> var <- get_variable(x, "b")

> stopifnot(!is.na(var))

> stopifnot(length(x) == 3)

> print(mapping(x))
 a  b  c 
NA NA NA 

> var <- get_variable(x, "c")

> stopifnot(!is.na(var))

> stopifnot(length(x) == 3)

> print(mapping(x))
 a  b  c 
NA NA NA 

> var <- get_variable(x, "d")

> stopifnot(!is.na(var))

> stopifnot(length(x) == 4)

> print(mapping(x))
  a   b   c   d 
 NA  NA  NA "d" 

> var <- get_variable(x, 4)

> stopifnot(!is.na(var))

> stopifnot(length(x) == 4)

> print(mapping(x))
  a   b   c   d 
 NA  NA  NA "d" 

> x$b <- 2

> var <- get_variable(x, "b")

> stopifnot(!is.na(var))

> stopifnot(length(x) == 4)

> print(mapping(x))
  a   b   c   d 
 NA "b"  NA "d" 

> var <- get_variable(x, length(x) + 1)

> stopifnot(length(x) == 5)

> print(names(x))
[1] "a" "b" "c" "d" "" 

> print(mapping(x))
               a                b                c                d 
              NA              "b"               NA              "d" 
                 
".listenv_var_1" 

> x <- listenv()

> length(x) <- 3

> print(x[[1]])
NULL

> print(x[[2]])
NULL

> print(x[[3]])
NULL

> res <- try(x[[0]], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(x[[4]], silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> print(get_variable(x, 1, mustExist = FALSE))
[1] ".listenv_var_1"

> print(get_variable(x, 2, mustExist = FALSE))
[1] ".listenv_var_2"

> print(get_variable(x, 3, mustExist = FALSE))
[1] ".listenv_var_3"

> res <- try(var <- get_variable(x, 0, mustExist = TRUE), 
+     silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(var <- get_variable(x, length(x) + 1, mustExist = TRUE), 
+     silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv()

> length(x) <- 3

> names(x) <- c("a", "b", "c")

> res <- try(var <- get_variable(x, "z", mustExist = TRUE), 
+     silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(var <- get_variable(x, c("a", "b")), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(var <- get_variable(x, 1 + (0+2i)), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> x <- listenv(a = 1, b = 2)

> var <- get_variable(x, "a", mustExist = TRUE)

> stopifnot(!is.na(var))

> x <- listenv()

> length(x) <- 3

> var <- get_variable(x, 2, create = FALSE)

> stopifnot(!is.na(var))

> stopifnot(length(x) == 3)

> x <- listenv()

> length(x) <- 2

> var <- get_variable(x, 5)

> stopifnot(length(x) == 5)

> x <- listenv(a = 1, b = 2)

> var <- get_variable(x, "a")

> stopifnot(!is.na(var))

> x <- listenv(a = 1)

> var <- get_variable(x, "z")

> stopifnot(!is.na(var))

> stopifnot(length(x) == 2)

> x <- as.listenv(1:6)

> dim(x) <- c(2, 3)

> res <- try(get_variable(x, c(1, 2, 3)), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(get_variable(x, c(NA, 1)), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> res <- try(get_variable(x, c(3, 1)), silent = TRUE)

> stopifnot(inherits(res, "try-error"))

> options(oopts)

> rm(list = setdiff(ls(envir = globalenv()), ovars), 
+     envir = globalenv())
Skipping, because path appears not to be an 'R CMD check' folder: ‘/tmp’
Test time: user.self=0.06s, sys.self=0.002s, elapsed=0.06s, user.child=0s, sys.child=0s
Test ‘get_variable’ ... success
> 
