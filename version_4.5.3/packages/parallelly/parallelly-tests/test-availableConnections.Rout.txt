
R version 4.5.3 (2026-03-11) -- "Reassured Reassurer"
Copyright (C) 2026 The R Foundation for Statistical Computing
Platform: x86_64-pc-linux-gnu

R is free software and comes with ABSOLUTELY NO WARRANTY.
You are welcome to redistribute it under certain conditions.
Type 'license()' or 'licence()' for distribution details.

R is a collaborative project with many contributors.
Type 'contributors()' for more information and
'citation()' on how to cite R or R packages in publications.

Type 'demo()' for some demos, 'help()' for on-line help, or
'help.start()' for an HTML browser interface to help.
Type 'q()' to quit R.

> #! /usr/bin/env Rscript
> ## This runs testme test script inst/testme/test-availableConnections.R
> ## Don't edit - it was autogenerated by inst/testme/deploy.R
> parallelly:::testme("availableConnections")
Test ‘availableConnections’ ...

> library(parallelly)

> message("availableConnections() ...")
availableConnections() ...

> options(parallelly.availableConnections = 200)

> n <- availableConnections()

> stopifnot(n == 200)

> options(parallelly.availableConnections = NULL)

> assign("max", NULL, envir = environment(availableConnections))

> res <- tryCatch({
+     options(parallelly.availableConnections = "abc")
+     availableConnections()
+ }, error = function(e) e)

> stopifnot(inherits(res, "simpleError"))

> options(parallelly.availableConnections = NULL)

> assign("max", NULL, envir = environment(availableConnections))

> options(parallelly.availableConnections.tries = 10)

> n <- availableConnections()

> stopifnot(is.infinite(n))

> options(parallelly.availableConnections.tries = NULL)

> assign("max", NULL, envir = environment(availableConnections))

> res <- tryCatch({
+     options(parallelly.availableConnections.tries = -1)
+     availableConnections()
+ }, error = function(e) e)

> stopifnot(inherits(res, "simpleError"))

> options(parallelly.availableConnections.tries = NULL)

> assign("max", NULL, envir = environment(availableConnections))

> n <- availableConnections()

> stopifnot(is.integer(n) || is.infinite(n), n >= 3)

> f <- freeConnections()

> stopifnot(is.integer(f) || is.infinite(f), f <= n)

> message("availableConnections() ... done")
availableConnections() ... done
Test time: user.self=0.08s, sys.self=0.004s, elapsed=0.08s, user.child=0s, sys.child=0s
Test ‘availableConnections’ ... success
> 
